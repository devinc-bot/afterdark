## 1. API startup configuration

- [x] 1.1 Replace `dotenv` loading with Node.js native `--env-file` startup options for API development and production, and remove the package dependency.

## 2. Verification

- [x] 2.1 Verify the API validates variables loaded from `apps/api/.env` for development and compiled startup; run API type-check, lint, format checks, and OpenSpec validation.
