---
description: Revise existing OpenSpec planning artifacts without implementing code.
agent: build
---

Use the `openspec-codex` skill in `update` mode. Change name or requested revision: $ARGUMENTS
