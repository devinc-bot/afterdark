---
description: Implement one verified task from an active SDD feature.
agent: build
---

Implement one unchecked task from the active SDD feature: $ARGUMENTS

Read its `spec.md`, `plan.md`, and `tasks.md` before editing. Follow the planned layer order: shared types and validators, database and migrations, API, then the affected client and i18n. Complete one layer-focused task, verify it with the relevant checks, and mark only that task complete after verification. Pause unless the user explicitly requests more.
