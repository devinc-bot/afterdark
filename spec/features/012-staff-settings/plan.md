# Plan de implementación — Configuración del staff

> Cómo se implementa esta feature. Complementa `spec.md`; no repetir criterios de aceptación.

## Orden de capas

```text
1. @afterdark/types       (BaseProfileResponse compartida, CurrentStaffResponse real)
2. @afterdark/validators  (baseProfileSchema + updateCurrentOwnerSchema/updateCurrentStaffSchema)
3. packages/db            (repositorios: findCurrentStaffByDocumentId, updateStaffProfileByDocumentId)
4. apps/api               (settings.controller.ts dispatch de schema por rol, settings.service.ts lógica real, error staff.INACTIVE)
5. @afterdark/i18n        (settings.shared.*, settings.staff.*, error staff.INACTIVE)
6. apps/dashboard         (mover building blocks a modules/settings/, genericizar, instanciar en modules/owner/ y modules/staff/)
```

## Archivos a crear / modificar

### Types

| Archivo                     | Cambio                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/types/src/api.ts` | Nueva `BaseProfileResponse { sub, name, lastName, email, avatar, role }`. `CurrentOwnerResponse extends BaseProfileResponse` (mismos campos propios que hoy, sin duplicar los 6 base). Reemplaza `StaffSettingsResponse` por `CurrentStaffResponse extends BaseProfileResponse { phone, status: StaffStatus }`. `SettingsResponse` sigue siendo `CurrentOwnerResponse \| CurrentStaffResponse`. |

### Validators

| Archivo                               | Cambio                                                                                                                                                                                                  |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/validators/src/owner.ts`    | Extraer `baseProfileSchema = z.object({ name, lastName, phone })` (mismas reglas que hoy). `updateCurrentOwnerSchema = baseProfileSchema.extend({ birthday, nationalId, taxId })`.                      |
| `packages/validators/src/staff.ts`    | Nuevo `updateCurrentStaffSchema = baseProfileSchema` (importado de `owner.ts`, o mover `baseProfileSchema` a `settings.ts` si se prefiere evitar el import cruzado owner→staff).                        |
| `packages/validators/src/settings.ts` | `settingsProfileSchema` deja de apuntar solo a `updateCurrentOwnerSchema`; pasa a ser un helper que expone ambos (`updateCurrentOwnerSchema`, `updateCurrentStaffSchema`) para que el controller elija. |

### Database

| Archivo                                            | Cambio                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/db/src/repositories/staff.repository.ts` | Nuevo `findCurrentStaffByDocumentId(documentId)` → `CurrentStaffRow` (agrega `status` a lo que ya trae `findStaffProfileByDocumentId`). Nuevo `updateStaffProfileByDocumentId(documentId, { name, lastName, phone })`. Nuevo `findStaffStatusByDocumentId(documentId)` (o reusar el row de `findCurrentStaffByDocumentId` antes del update) para el chequeo `INACTIVE` en el service. |

Sin cambios de schema Drizzle — `staff.name/lastName/phone/avatar/status` ya existen.

### API

| Archivo                                                  | Cambio                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/modules/settings/settings.controller.ts`   | `PATCH /settings` deja de usar `ZodValidationPipe(updateCurrentOwnerSchema)` fijo. Recibe `@Body() body: unknown` (o `Record<string, unknown>`) + `@CurrentUser() user`, elige `updateCurrentOwnerSchema`/`updateCurrentStaffSchema` según `user.role` y llama `.parse()` (o pipe construido dinámicamente) antes de delegar en `SettingsService`. |
| `apps/api/src/modules/settings/settings.service.ts`      | `role === STAFF`: `getSettings` → `findCurrentStaffByDocumentId` (404 `staff.NOT_FOUND` si no existe, mismo criterio que owner). `updateSettings` → valida `status === ACTIVE` (403 `staff.INACTIVE` si no), llama `updateStaffProfileByDocumentId`, devuelve `getSettings` actualizado.                                                           |
| `apps/api/src/modules/staff/staff.service.ts` (opcional) | Si se prefiere no acoplar `SettingsService` directo a repos de `staff.repository.ts`, exponer `StaffService.getCurrentStaff`/`updateCurrentStaff` (mismo patrón que `OwnerService`) e inyectarlo en `SettingsModule`. Recomendado por simetría con owner.                                                                                          |
| `apps/api/src/modules/settings/settings.module.ts`       | Si se agrega `StaffService` como dependencia, importar `StaffModule` (ya exporta `StaffService`) o declarar el provider ahí mismo.                                                                                                                                                                                                                 |

### i18n (`packages/i18n`)

| Archivo                                    | Cambio                                                                                                                                                                                                                                                                 |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/constants/error-codes.ts`             | `STAFF_ERROR_CODE` agrega `INACTIVE: 'staff.INACTIVE'`.                                                                                                                                                                                                                |
| `src/locales/errors/es.json` y `en.json`   | Agregar mensaje para `staff.INACTIVE` bajo el bloque `staff` existente (junto a `NOT_FOUND`).                                                                                                                                                                          |
| `src/locales/settings/es.json` y `en.json` | Mover `actions.*`/`messages.*` de `owner` a `shared` (reusadas por ambos roles). `staff` deja de tener solo `page.title`/`placeholder`: agrega `page.description`, `sections.profile`, `profile.name/lastName/phone/email/avatarFallback`. Quitar `staff.placeholder`. |

### Client (`dashboard`)

| Archivo                                                                                           | Cambio                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/modules/settings/hooks/settings-form-context.tsx` (nuevo, movido de `owner/`)                | `createSettingsFormProvider<TUser extends BaseProfileResponse, TProfile>({ toFormValues, validate, mapErrors, updateFn })` — factory que devuelve `{ Provider, useSettingsForm }` genéricos. Reemplaza el `SettingsFormProvider` hardcodeado a `CurrentOwnerResponse`.                                                 |
| `app/modules/settings/hooks/use-settings-form-values.ts` (nuevo, movido de `owner/`)              | Genérico sobre `TProfile` (ya casi lo es hoy — solo pierde el import directo a `CurrentOwnerResponse`).                                                                                                                                                                                                                |
| `app/modules/settings/components/settings-form-actions.tsx` (nuevo, movido)                       | Usa `useTranslation('settings')` con claves `shared.actions.*` en vez de `owner.actions.*`. Recibe el hook de contexto específico por instancia (o usa un contexto genérico pasado por prop/composición).                                                                                                              |
| `app/modules/settings/components/settings-status-banner.tsx` (nuevo, movido)                      | Ídem, `shared.messages.*`.                                                                                                                                                                                                                                                                                             |
| `app/modules/settings/components/settings-section.tsx` (nuevo, movido)                            | Sin cambios de lógica, deja de ser específico de owner.                                                                                                                                                                                                                                                                |
| `app/modules/settings/constants/settings-form.ts` (nuevo, movido)                                 | `SETTINGS_FORM_ID`, `SETTINGS_SAVE_STATUS`, etc. — constantes compartidas.                                                                                                                                                                                                                                             |
| `app/modules/settings/utils/settings-form.utils.ts`, `settings-storage.utils.ts` (movidos)        | Genéricos sobre `TProfile`/schema pasado por parámetro en vez de importar `updateCurrentOwnerSchema` fijo.                                                                                                                                                                                                             |
| `app/modules/owner/hooks/*`, `components/settings-form-actions.tsx`, `settings-status-banner.tsx` | Se eliminan (reemplazados por los genéricos de `modules/settings/`). `owner-settings-view.tsx` instancia `createSettingsFormProvider<CurrentOwnerResponse, OwnerProfileFields>(...)` con `updateCurrentOwnerSchema`.                                                                                                   |
| `app/modules/owner/components/profile-settings-section.tsx`                                       | Sin cambios de campos/UI — pasa a usar el hook genérico (`useSettingsForm` de la instancia owner) en vez del específico.                                                                                                                                                                                               |
| `app/modules/staff/components/staff-settings-view.tsx`                                            | Deja de ser placeholder: instancia `createSettingsFormProvider<CurrentStaffResponse, StaffProfileFields>(...)` con `updateCurrentStaffSchema`, renderiza `<StaffProfileSettingsSection />` + `SettingsFormActions`/`SettingsStatusBanner` (de `modules/settings/`) dentro del mismo `form`/`FormLayout` que usa owner. |
| `app/modules/staff/components/staff-profile-settings-section.tsx` (nuevo)                         | Campos: nombre, apellido, teléfono editables; avatar (imagen o iniciales, sin botón "cambiar foto" activo) y email de solo lectura. Sin fecha nac./DNI/CUIT-CUIL/password (no existen para staff).                                                                                                                     |
| `app/modules/settings/components/settings-view.tsx` (dispatcher)                                  | `data.role === 'staff'` → `<StaffSettingsView staff={data} />` en vez de `<StaffSettingsView />` sin props (ahora recibe los datos ya resueltos, igual que owner).                                                                                                                                                     |

## Diseño técnico

- **Generics reales, no solo "compartir componentes".** `createSettingsFormProvider<TUser extends BaseProfileResponse, TProfile>()` es una factory de contexto: cada rol la invoca una vez con su propio tipo de usuario, tipo de valores de formulario y función de guardado (`updateSettings` sigue siendo un solo endpoint `PATCH /settings`, pero el schema/campos que arma el body cambian por instancia). Evita un único provider gigante con if-role-then-branch adentro.
- **Validación de rol en dos lugares, igual que 004**: el controller elige el schema Zod por `user.role` (seguridad — no confía en qué campos manda el cliente), el service elige la lógica de persistencia por `user.role` (ya existía). No se unifica en un único "mega-schema opcional" porque eso permitiría a un owner mandar body de staff incompleto y viceversa sin error 400 claro.
- **`status === INACTIVE` se chequea en `SettingsService`, no en un guard HTTP genérico** — es una regla específica del recurso "perfil propio de staff", no una regla transversal de autenticación (`JwtAuthGuard` ya cubre "sesión válida"). Mismo criterio que otros chequeos de `status` existentes en `staff.service.ts` (ahí es el owner gestionando a su staff; acá es el propio staff).
- **`BaseProfileResponse` vive en `@afterdark/types`, no como generic parametrizado en runtime** — es composición de interfaces TypeScript, sin lógica de runtime. El generic con lógica real vive en el frontend (`createSettingsFormProvider`) y en el schema Zod (`baseProfileSchema.extend(...)`).

## Riesgos / edge cases

| Caso                                                                                      | Comportamiento esperado                                                                                                                                                                                |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Staff con `status = INACTIVE` intenta `PATCH /settings`                                   | 403 `staff.INACTIVE`. `GET /settings` sigue funcionando (puede ver sus datos, no editarlos).                                                                                                           |
| Staff manda `birthday`/`nationalId`/`taxId` en el body de `PATCH /settings`               | `updateCurrentStaffSchema` (sin esos campos) los ignora si no es `.strict()`, o rechaza con 400 si se decide usar `.strict()` — a resolver en implementación, no cambia el contrato de campos válidos. |
| Owner y staff comparten `baseProfileSchema` con los mismos mensajes de validación         | OK — mismos límites de `name`/`lastName`/`phone` para ambos roles, confirmado en Fase 4.                                                                                                               |
| Refactor de `modules/owner/hooks/*` → `modules/settings/hooks/*` rompe imports existentes | Verificar con `pnpm type-check` en `dashboard`; no debe quedar ningún `~/modules/owner/hooks/settings-form-context` residual.                                                                          |
| `SettingsModule` (API) pasa a depender de `StaffModule` además de `OwnerModule`           | Confirmar que no se forma import circular (`StaffModule` no importa `SettingsModule`) — no debería, `StaffModule` ya es independiente.                                                                 |

## Verificación manual

| Paso                                                                                               | Resultado esperado                                                                                                  |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 1. Login como staff activo, ir a `/settings`                                                       | Formulario real con nombre/apellido/teléfono editables, avatar/email solo lectura — ya no dice "Hello World".       |
| 2. Editar nombre y guardar                                                                         | `PATCH /settings` OK, confirmación visual, recargar mantiene el cambio.                                             |
| 3. `GET /settings` con JWT de staff (curl/Postman)                                                 | Devuelve `CurrentStaffResponse` completo (`sub`, `name`, `lastName`, `email`, `avatar`, `phone`, `status`, `role`). |
| 4. Marcar staff como `INACTIVE` (vía owner en `/staff`) y reintentar `PATCH /settings` con ese JWT | 403 con mensaje `staff.INACTIVE` en español.                                                                        |
| 5. Login como owner, ir a `/settings`                                                              | Sin regresión — formulario de owner funciona igual que antes del refactor de building blocks compartidos.           |
| 6. `pnpm type-check` en `api` y `dashboard`                                                        | Sin errores tras mover `modules/owner/hooks/*` a `modules/settings/hooks/*`.                                        |
